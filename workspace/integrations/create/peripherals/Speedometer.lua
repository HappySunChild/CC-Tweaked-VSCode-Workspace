---@meta

---@class create.peripheral.Speedometer
local Speedometer = {
	---Returns the current rotation speed of the attached components.
	---@return integer speed The current rotation speed in RPM.
	getSpeed = function() end,
}
